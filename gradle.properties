org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# ---------------------------------------------------------------------------
# ЕДИНСТВЕННОЕ место, где задаётся адрес витрины магазина.
# Отсюда значение попадает в BuildConfig.MANIFEST_URL (см. app/build.gradle.kts)
# и дальше в StoreConfig. Замените владельца репозитория на своего.
# ---------------------------------------------------------------------------
MANIFEST_URL=https://raw.githubusercontent.com/khudobin-v/app-store-manifest/main/apps.json
