org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# ---------------------------------------------------------------------------
# Адрес витрины по умолчанию (можно переопределить на устройстве).
# Отсюда значение попадает в BuildConfig.MANIFEST_URL (см. app/build.gradle.kts)
# и дальше в StoreConfig. Замените владельца репозитория на своего.
# ---------------------------------------------------------------------------
MANIFEST_URL=https://personal-app-store-vert.vercel.app/api/apps
